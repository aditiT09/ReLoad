{
  "name": "reload-workspace",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:customer": "npm --prefix customer-app run dev",
    "dev:driver": "npm --prefix driver-app run dev",
    "build:customer": "npm --prefix customer-app run build",
    "build:driver": "npm --prefix driver-app run build"
  }
}